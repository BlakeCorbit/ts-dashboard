# Zendesk API
ZENDESK_SUBDOMAIN=bayiq
ZENDESK_EMAIL=your-email@autovitalsinc.com
ZENDESK_API_TOKEN=your-zendesk-api-token

# Polling
POLL_INTERVAL_SECONDS=30

# Set to true to actually update tickets in Zendesk (add tags, internal notes)
# Set to false for dry-run (console output only)
LIVE_MODE=false
